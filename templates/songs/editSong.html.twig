{% extends 'base.html.twig' %}

{% block title %}My Tunes Lib - Modifier un titre
{% endblock %}

{% block body %}
	<h2>Modifier un titre</h2>
	{% for message in app.flashes('error') %}
		<div style="color:red">
			{{ message }}
		</div>
	{% endfor %}

	{{ form_start(form) }}
	{{ form_widget(form) }}
	{{ form_end(form) }}
{% endblock %}